// Create our Banner function-based component.

export const Banner = () => {
  return (
    <div className="container text-center p-3">
      <h1 className="p-3 fw-semibold">
        An awesome counter app as never done before
      </h1>
      <h2 className="p-3 fs-3">Start counting by using the buttons below</h2>
    </div>
  );
};
